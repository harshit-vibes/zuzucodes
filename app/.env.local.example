# Neon Database
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Neon Auth (from Neon Console → Auth)
# Get these values from your Neon project's Auth settings
NEON_AUTH_BASE_URL=https://your-auth-endpoint.neonauth.c-4.us-east-1.aws.neon.tech
NEON_AUTH_COOKIE_SECRET=generate_with_openssl_rand_base64_32

# App Configuration
NEXT_PUBLIC_ROOT_DOMAIN=zuzu.codes
NEXT_PUBLIC_APP_URL=https://zuzu.codes

# Optional: Neon Auth Email Provider (if using custom email provider)
# NEON_AUTH_EMAIL_FROM=noreply@zuzu.codes
